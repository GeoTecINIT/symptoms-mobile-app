import { AndroidLocationProvider } from '~/app/core/providers/location/location-provider.android';

describe('Android Location Provider', () => {
    const locationProvider = new AndroidLocationProvider();

    it('returns if it is enabled or not', () => {
        throw new Error('Not implemented');
    });

    it('allows to enable the provider', () => {
        throw new Error('Not implemented');
    });

    it('returns at least one locations when requested', () => {
        throw new Error('Not implemented');
    });

    it('returns more than one location when requested', () => {
        throw new Error('Not implemented');
    });

    it('returns the locations gathered until the time the request gets canceled', () => {
        throw new Error('Not implemented');
    });
});
